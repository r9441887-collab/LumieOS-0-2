[workspace]
members = [
    "kernel",
    "loader",
    "shell",
    "editor",
    "desktop",
    "libs/lumie_std",
    "libs/lumie_math",
]
resolver = "2"

[profile.dev]
panic = "abort"

[profile.release]
lto = true
opt-level = "z"
codegen-units = 1
strip = "symbols"
panic = "abort"
