[workspace]
members = [
    "kernel",
    "loader",
    "shell",
    "editor",
    "desktop",
    "libs/lumie_std",
]
resolver = "2"

[profile.release]
lto = true
opt-level = "z"
codegen-units = 1
strip = "symbols"
panic = "abort"
